import { GradeScheduleOverride } from './grade_schedule_override';

describe('GradeScheduleOverride', () => {
  it('should be defined', () => {
    expect(new GradeScheduleOverride()).toBeDefined();
  });
});
